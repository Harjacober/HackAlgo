#!/bin/sh
mkdir -p /var/run/celery
mkdir -p /var/log/celery
python db/create.py
touch /var/log/celery/log.log
#celery multi start w1 -A coderunner.celerytasks -l info --pidfile=/var/run/celery/%n.pid \
#                                        --logfile=/var/log/celery/log.log --loglevel=DEBUG --verbose

celery -A api.access.contest worker -l info --loglevel=DEBUG &
export TESTING=1
#python test/test_app.py
export TESTING=0
python app.py 
